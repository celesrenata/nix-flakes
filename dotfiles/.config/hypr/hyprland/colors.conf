# exec = export SLURP_ARGS='-d -c dae2f9BB -b 3f475944 -s 00000000'

general {
    col.active_border = rgba(e4e2e639)
    col.inactive_border = rgba(8e909930)
}

misc {
    background_color = rgba(1b1b1eFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(101012FF)
        col.text = rgba(e4e2e6FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(e4e2e6), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(e4e2e6), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(e4e2e6), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(acc7ffAA) rgba(acc7ff77),pinned:1